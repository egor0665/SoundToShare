<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/mobile_navigation"
        app:startDestination="@id/home">

        <fragment
                android:id="@+id/home"
                android:name="com.example.soundtoshare.fragments.home.HomeFragment"
                android:label="home"
                tools:layout="@layout/fragment_home" />

        <fragment
                android:id="@+id/map"
                android:name="com.example.soundtoshare.fragments.map.MapFragment"
                android:label="map"
                tools:layout="@layout/fragment_map" />

        <fragment
                android:id="@+id/settings"
                android:name="com.example.soundtoshare.fragments.settings.SettingsFragment"
                android:label="settings"
                tools:layout="@layout/fragment_settings" />
</navigation>